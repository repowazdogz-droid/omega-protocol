/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow importing TypeScript files from spine directory
  transpilePackages: [],
  
  // Experimental features if needed
  experimental: {
    // Enable if needed for better TypeScript support
  }
}

module.exports = nextConfig











